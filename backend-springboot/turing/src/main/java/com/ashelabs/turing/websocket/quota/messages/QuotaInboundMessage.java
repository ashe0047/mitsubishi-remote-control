package com.ashelabs.turing.websocket.quota.messages;

/**
 * Base interface for all inbound (client → server) quota WebSocket messages.
 * Implementations are in the inbound subpackage.
 */
public interface QuotaInboundMessage {
}
