package com.ashelabs.turing.websocket.quota.messages;

/**
 * Base interface for all outbound (server → client) quota WebSocket messages.
 * Implementations are in the outbound subpackage.
 */
public interface QuotaOutboundMessage {
}
